{
  "name": "ndr-tidb-server",
  "version": "1.0.0",
  "private": true,
  "description": "TiDB-backed REST API for the NDR EDTECH Student Portal — same action contract as apps-script/ and mock-server/, backed by a real TiDB (MySQL-wire-compatible) database instead of Google Sheets.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "node setup.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.11.0"
  }
}
